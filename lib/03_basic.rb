

hash = { A: 84, B: 42, C: "nil" }

def who_is_bigger(hash)
    hash.each { |"A", "B", "C"| puts "#{A} : #{B} : #{C}" if values == hash.values.max }
    end
puts "is bigger"
end

def less_than(hash, value)
new_hash = {}
nb = 0
hash.each do |k, v|
		if v < value
            puts "ok"
        else 
        puts "NON"
        end
    end
end

who_is_bigger(hash)